# Tempo — хранилище трейсов. Сервисы шлют спаны по OTLP (HTTP :4318),
# Grafana читает их через HTTP API (:3200).
stream_over_http_enabled: true

server:
  http_listen_port: 3200
  log_level: warn

distributor:
  receivers:
    otlp:
      protocols:
        http:
          endpoint: "0.0.0.0:4318"
        grpc:
          endpoint: "0.0.0.0:4317"

storage:
  trace:
    backend: local
    wal:
      path: /var/tempo/wal
    local:
      path: /var/tempo/blocks

# Сколько хранить трейсы. Старые блоки удаляет фоновый compaction.
backend_scheduler:
  provider:
    compaction:
      compaction:
        block_retention: 168h   # 7 дней

backend_worker:
  compaction:
    block_retention: 168h

usage_report:
  reporting_enabled: false
