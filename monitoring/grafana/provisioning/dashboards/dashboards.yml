apiVersion: 1

providers:
  - name: rbs
    folder: RBS
    type: file
    disableDeletion: false
    allowUiUpdates: true      # можно править в UI, но изменения затрутся при рестарте — переносить в json
    options:
      path: /etc/grafana/provisioning/dashboards/json
