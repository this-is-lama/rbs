apiVersion: 1

policies:
  - orgId: 1
    receiver: telegram
    group_by: ['grafana_folder', 'alertname']
    group_wait: 30s        # подождать, чтобы собрать одновременные алерты в одно сообщение
    group_interval: 5m
    repeat_interval: 4h    # напоминать, пока проблема не решена
