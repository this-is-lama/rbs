apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus          # на этот uid ссылаются алерты и дашборды
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
